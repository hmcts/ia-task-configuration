#!groovy

properties([
  pipelineTriggers([cron('H 8 * * 1-5')]),
])

@Library("Infrastructure")

def type = "java"
def product = "ia"
def component = "task-configuration"

withNightlyPipeline(type, product, component) {
  enableDockerTestBuild()
}
